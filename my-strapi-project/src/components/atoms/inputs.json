{
  "collectionName": "components_atoms_inputs",
  "info": {
    "displayName": "inputs"
  },
  "options": {},
  "attributes": {
    "label": {
      "type": "string",
      "required": true
    },
    "name": {
      "type": "string",
      "required": true,
      "regex": "^[A-Za-z0-9_-]+$"
    },
    "required": {
      "type": "boolean",
      "required": true,
      "default": true
    },
    "type": {
      "type": "enumeration",
      "required": true,
      "enum": [
        "text",
        "select",
        "optin"
      ]
    },
    "options": {
      "type": "json",
      "conditions": {
        "visible": {
          "==": [
            {
              "var": "type"
            },
            "select"
          ]
        }
      }
    }
  },
  "config": {}
}
